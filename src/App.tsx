import { BrowserRouter } from 'react-router-dom'

import AppContainer from '@/components/AppContainer'

const App = () => {
	return (
		<BrowserRouter>
			<AppContainer />
		</BrowserRouter>
	)
}

export default App
